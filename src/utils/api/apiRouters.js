const baseRouter = "/api/v1";

export const ApiRouters = {
  STATICPAGES: `${baseRouter}/StaticPages`,
  MEMBERSHIPS: `${baseRouter}/Memberships`,
  WARRANTY_REGISTER: `${baseRouter}/warranty-register`,
};
